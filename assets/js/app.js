function irArriba() { /* aca cree la funcion de irArriba (camelCase) */
    window.scrollTo({ 
        top: 0, /* aca le decimos a que altura tiene que llegar. siendo 0 el inicio de la pagina*/
        behavior: "smooth" /* Define como se tiene que mover*/
    });
}